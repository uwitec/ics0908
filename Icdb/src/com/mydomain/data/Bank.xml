<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Bank">

	<select id="selectAllFind" resultClass="com.mydomain.bean.BankBean">
	SELECT
	BANKCODE,
	BANKNAME
	FROM BANK
	WHERE BANKNAME LIKE #bankName# and isEnabled=1
	</select>
	<select id="getBankBean" parameterClass="int" resultClass="com.mydomain.bean.BankBean">
		SELECT 
		BANKCODE,
		BANKNAME
		FROM BANK
		WHERE BANKCODE=#bankCode# and isEnabled=1
	</select>
	<select id="selectAllBankBean" resultClass="com.mydomain.bean.BankBean">
		SELECT 
		BANKCODE,
		BANKNAME
		FROM BANK
		where isEnabled=1
	</select>
	<insert id="insertBankBean" parameterClass="com.mydomain.bean.BankBean">
		INSERT INTO 
		BANK
		(BANKCODE,BANKNAME,isEnabled) VALUES
		(#bankCode#,#bankName#,1)
	</insert>
	<update id="updateBankBean" parameterClass="com.mydomain.bean.BankBean">
		UPDATE BANK
		SET
		BANKNAME=#bankName#
		WHERE bankCode=#bankCode#
	</update>
	<delete id="deleteBankBean" parameterClass="com.mydomain.bean.BankBean">
		UPDATE BANK
		SET
		isEnabled=0
		WHERE bankCode=#bankCode#
	</delete> 
</sqlMap>