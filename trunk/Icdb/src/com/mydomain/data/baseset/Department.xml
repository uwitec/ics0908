<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Department">
	<select id="selectDepartmentCount" resultClass="com.mydomain.bean.baseset.DepartmentBean">
		SELECT
		count(*) as countValue
		FROM department,struct
		WHERE 
		department.isEnabled=1
		and
		department.structCode=struct.structCode
		and(
		locate(#departmentCode#,departmentCode)>0 
		or
		locate(#departmentName#,departmentName)>0
		or
		locate(#structName#,struct.structName)>0)
	</select>
	<select id="selectDepartmentDef" resultClass="com.mydomain.bean.baseset.DepartmentBean">
		SELECT
		departmentCode,
		departmentName,
		department.structCode,
		structName
		FROM department,struct
		WHERE 
		department.isEnabled=1
		and
		department.structCode=struct.structCode
		and(
		locate(#departmentCode#,departmentCode)>0 
		or
		locate(#departmentName#,departmentName)>0
		or
		locate(#structName#,struct.structName)>0)
		limit #startValue#,#endValue#
	</select>
	<select id="getDepartmentBean" parameterClass="String" resultClass="com.mydomain.bean.baseset.DepartmentBean">
		SELECT
		departmentCode,
		departmentName,
		department.structCode,
		structName
		FROM department,struct
		WHERE 
		department.structCode=struct.structCode
		AND
		department.isEnabled=1
		and
		departmentCode = #departmentCode#
	</select>
	<insert id="insertDepartmentBean" parameterClass="com.mydomain.bean.baseset.DepartmentBean">
		INSERT INTO
		department
		(departmentCode,
		departmentName,
		structCode,isEnabled)
		VALUES (#departmentCode#,#departmentName#,#structCode#,1)
	</insert>
	<update id="updateDepartmentBean" parameterClass="com.mydomain.bean.baseset.DepartmentBean">
		UPDATE department
		SET
		departmentName=#departmentName#,
		structCode=#structCode#,
		isEnabled=1
		WHERE departmentCode= #departmentCode#
	</update>
	<delete id="deleteDepartmentBean" parameterClass="com.mydomain.bean.baseset.DepartmentBean">
		UPDATE department
		SET
		isEnabled=1
		WHERE departmentCode= #departmentCode#
	</delete>
</sqlMap>